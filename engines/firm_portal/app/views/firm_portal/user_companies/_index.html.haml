- @user_companies.each do |u|
  %tr
    %td.col-md-4= u.name
    %td.col-md-2= u.email
    %td.col-md-2= u.login_user
    %td.col-md-2
      = link_to "editar".html_safe, edit_user_company_path(u), remote: true, class: " btn btn-info"
