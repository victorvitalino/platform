= simple_form_for([@ticket_type, @ticket_attendant]) do |f|
  .card-body.card-padding
    .row
      .col-md-6
        = f.label :staff, class: 'fg-label'
        = f.association :staff, label: false, prompt: "Selecione o funcionário..."
    .row
      .col-md-12
        .toggle-switch{"data-ts-color" => "blue"}
          %label
            .ts-label{:for => "ts4"}
              = f.label :status
          %br
          %br
          = f.check_box :status, id: 'ts4',hidden: 'hidden', type: 'checkbox'
          %label.ts-helper{:for => "ts4"}
  
    %hr/
    .row
      .col-md-12
        = f.button :submit, class: 'btn btn-success'
        = link_to "voltar", ticket_type_ticket_attendants_path(@ticket_type), class: 'btn btn-inverse'