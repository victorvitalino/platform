<%= form_for(@monitor_service_order) do |f| %>
  <% if @monitor_service_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monitor_service_order.errors.count, "error") %> prohibited this monitor_service_order from being saved:</h2>

      <ul>
      <% @monitor_service_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :appointment %><br>
    <%= f.text_area :appointment %>
  </div>
  <div class="field">
    <%= f.label :attachment %><br>
    <%= f.text_field :attachment %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
