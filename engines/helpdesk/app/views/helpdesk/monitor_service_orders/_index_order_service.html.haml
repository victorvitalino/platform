= simple_form_for(@order_service) do |f|
 .card
  .card-header.bgm-red
   %h2
    Ordem de serviço #
    = @order_service.number
   %ul.actions
    %li
     = link_to '<i class="md md-thumb-up c-white"></i>'.html_safe, order_service_monitor_service_order_assume_path(@order_service.id, @monitor_service_order.id), remote: true, class: ' pull-right'
  .card-body.card-padding
   .row
    .col-md-8
     = f.label "Aperto por:", class: 'fg-label'
     = @order_service.opened_by.name
     %br
     = f.label "Solicitante:", class: 'fg-label'
     = @order_service.staff.name
     %br
     = f.label "Setor:", class: 'fg-label'
     = @order_service.sector.name
     %br
     = f.label "Status:", class: 'fg-label'
     = @order_service.status.name
     %br
     = f.label "Ramal:", class: 'fg-label'
     = @order_service.branch_line.telephone
     %br
     = f.label "Responsável:", class: 'fg-label'
     = @order_service.responsible.name if @order_service.responsible.present? 
    .col-md-4
     .card
      .card-header
       %h6
        = f.label "Etiqueta", class: 'fg-label'
        = @order_service.good.code_goods
       %h6
        = f.label "Material:", class: 'fg-label'
        = @order_service.good.material.name 
       %h6
        = f.label "Nº de Seriel: 851984", class: 'fg-label'
   

  
    