- if @order_service.errors.any?
	$('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
	$('#modal').modal('hide')
	$('.index-order_service').html("#{escape_javascript(render 'index')}")
