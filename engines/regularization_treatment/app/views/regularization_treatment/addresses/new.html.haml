= render 'layouts/regularization_treatment/steps'
.row
  .col-md-12
    .card
      .card-body.card-padding
        .row
          .col-md-12{:style => 'text-align: center; margin-top: 45px'}
            %h3
              %b Endereço a ser regularizado
            %h1
              = @requeriment.unit.city.name if @requeriment.unit.present? && @requeriment.unit.city.present?
              = @requeriment.unit.complete_address if @requeriment.unit.present?
        .row{:style => 'margin-top: 85px'}
          .col-md-12
            %span.pull-right
              = link_to "Voltar para consulta", new_consult_path(cpf: @requeriment.cpf), class: 'btn btn-inverse'
              = link_to "Prosseguir", requeriment_cadastre_step_path(@requeriment), class: 'btn btn-primary'  
