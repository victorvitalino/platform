.block-header
  %h2 
    %b 
      = link_to @project.name, administrator_projects_path
      \/ Tipos de inscrições /
      = link_to @enrollment.title, administrator_project_enrollments_path(project_id: @project)
    
.card
  .card-header.ch-alt
    %span.pull-right
      = link_to "voltar", concourse.administrator_project_enrollments_path(@project), class: 'btn btn-default'
      = link_to "novo campo", concourse.new_administrator_project_enrollment_enrollment_field_path(project_id: @project, enrollment_id: @enrollment), remote: true, class: 'btn btn-success'
    %h2
      Campos
      %small
        Listagem de campos para inscrição
  .table-responsive{:style => "overflow: hidden; outline: none;", :tabindex => "4"}
    %table.table.table-condensed
      %thead
        %tr
          %th #
          %th Nome
          %th Tipo
          %th Attributo
          %th Obrigatório?
          %th Status
          %th Ações
      %tbody.index-field
        = render 'index'