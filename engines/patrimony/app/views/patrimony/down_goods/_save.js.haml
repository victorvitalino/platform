- if @down_good.errors.any?
	$('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
	$('#modal').modal('hide')
	$('.index-down_good').html("#{escape_javascript(render 'index')}")
