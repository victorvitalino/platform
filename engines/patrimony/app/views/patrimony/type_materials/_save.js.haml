- if @type_material.errors.any?
	$('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
	$('#modal').modal('hide')
	$('.index-type_material').html("#{escape_javascript(render 'index')}")
