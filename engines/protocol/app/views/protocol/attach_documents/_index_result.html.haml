%br
.table-responsive{:style => "overflow: hidden; outline: none;", :tabindex => "3"}
  %table#data-table-command.table.table-striped.table-bordered
    %thead
      %tr
        %th Número
        %th Tipo de Documento
        %th Setor
        %th Tipo
        %th Excluir
    %tbody
      - @attach_documents.each do |a|
        %tr
          %td
            = a.document_child.document_number if a.document_child.present?
          %td
            = a.document_child.document_type.name if a.document_child.present?
          %td
            = a.sector.name if a.sector.present?
          %td
            = a.attach_type_i18n
          %td
            = link_to "", assessment_attach_document_path(a.document_child.id,a.id), method: :delete, remote:true, class: 'glyphicon glyphicon-remove btn btn-danger btn-xs', data: {confirm: "#{t(:confirm_delete)}"}
