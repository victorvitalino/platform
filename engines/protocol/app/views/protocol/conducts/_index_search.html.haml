- if @conduct_result.present?
  %span.c-black Documento Pesquisado
  %br
  .table-responsive{:style => "overflow: hidden; outline: none;", :tabindex => "3"}
    %table.table.table-hover.table-bordered
      %thead
        %tr
          %th Número
          %th Tipo de Documento
          %th Setor
          %th Incluir na Remessa
      %tbody
        - @conduct_result.each do |a|
          %tr
            %td
              = a.document_number
            %td
              = a.document_type.name
            %td
              = a.sector.name
            %td
              = link_to "", add_path(assessment_id: a.id, id: @conduct.allotment.id), title: 'Gerar Remessa', 'data-toggle' => 'tooltip',  class: 'md-check btn btn-info waves-effect waves-button waves-float'