%span.c-black Documento Inserido na Remessa
%br
.table-responsive{:style => "overflow: hidden; outline: none;", :tabindex => "3"}
  %table.table.table-hover.table-bordered
    %thead
      %tr
        %th Número
        %th Tipo de Documento
        %th Setor
        %th Excluir da Remessa
    %tbody
      - @conducts.each do |a|
        %tr
          %td
            = a.assessment.document_number
          %td
            = a.assessment.document_type.name
          %td
            = a.sector.name if a.sector.present?
          %td
            = link_to "", allotment_conduct_path(@conduct.allotment.id,a.id), method: :delete, remote:true, class: 'md-highlight-remove btn btn-danger waves-effect waves-button waves-float', data: {confirm: "#{t(:confirm_delete)}"}
