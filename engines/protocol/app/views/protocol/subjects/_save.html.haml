- if @subject.errors.any?
  $('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
  $('#modal').modal('hide')
  $('.index-subject').html("#{escape_javascript(render 'index')}")
