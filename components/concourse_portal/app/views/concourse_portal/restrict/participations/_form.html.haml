= simple_form_for @participation, url: project_restrict_participations_path(project_id: @project) do |f|
  .row
    .col-md-12
      = f.label "Membros da sua equipe"
      = f.input :team_description, label: false, input_html: { placeholder: "Insira os nomes dos integrantes da sua equipe: Maria, Jonas, Pedro, Marcelo, Fernanda.. etc"}
  %hr/
  .row
    .col-md-5
      = f.input :archive_one, label: "Pranchas A3 em um único arquivo PDF(máximo de 18MB)", as: :file
  .row
    .col-md-5
      = f.input :archive_two, label: "Prancha A2 (PDF, máximo de 3MB)", as: :file
  %hr/
  .row
    .col-md-12
      = @project.text_participation.html_safe
  %hr/
  .row
    .col-md-12
      = f.submit "Enviar meu projeto", class: 'btn btn-success', data: { disable_with: t(:disable_with)}