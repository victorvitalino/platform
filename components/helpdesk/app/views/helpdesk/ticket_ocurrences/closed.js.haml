- if @ticket_ocurrence.errors.any?
  $('#modal .modal-content').html("#{escape_javascript(render 'closed_ticket')}")
- else
  $('#modal').modal('hide')
  $('.index-ticket').html("#{escape_javascript(render '/helpdesk/tickets/index')}")
