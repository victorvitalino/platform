- if @procedural_status.errors.any?
  $('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
  $('#modal').modal('hide')
  $('.index-procedural_status').html("#{escape_javascript(render 'index')}")
