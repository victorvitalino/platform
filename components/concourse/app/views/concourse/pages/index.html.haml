.panel
  .panel-heading
    .panel-title
      %h3
        Páginas
        = link_to "Nova página", new_project_page_path(@project), class: 'btn btn-success btn-sm pull-right'
  .panel-body
    %table.table.table-striped.table-bordered.table-hover
      %thead
        %tr
          %th Título
          %th Publicada?
          %th Data
          %th URL
          %th Ações
      %tbody
        - @project.pages.each_with_index do |page, index|
          %tr
            %td.col-md-2= page.title
            %td= page.publish ? 'publicada' : 'não publicada'
            %td= page.created_at.strftime('%d/%m/%Y')
            %td= "/concursos/#{@project.slug}/pagina/#{page.slug}"
            %td.col-md-1
              = link_to "", edit_project_page_path(project_id: @project, id: page), data: { turbolinks: false} ,class: 'glyphicon glyphicon-pencil btn btn-primary btn-xs'
              = link_to "", project_page_path(project_id: @project, id: page), method: :delete,class: 'glyphicon glyphicon-remove btn btn-danger btn-xs', :data => { :confirm => t(:confirm_delete) } 