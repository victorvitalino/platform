- if @call_control.errors.any?
  $('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
  $('#modal').modal('hide')
  $('.index-call').html("#{escape_javascript(render 'index')}")
