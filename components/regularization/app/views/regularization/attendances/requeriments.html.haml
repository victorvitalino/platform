%table.table.table-striped.table-bordered
  %thead
    %tr
      %th CPF
      %th Endereço
      %th Cidade
      %th Data Requerimento
      %th Ações
  %tbody
    - @cadastre.requeriments.each do |requeriment|
      %tr
        %td= requeriment.cpf
        %td= requeriment.unit.complete_address if requeriment.unit.present?
        %td= requeriment.unit.city.name if requeriment.unit.city.present?
        %td= requeriment.created_at.strftime('%d/%m/%Y ás %H:%M')
        %td.col-md-2
          = link_to "Realizar Atendimento", next_wizard_path(requeriment_id: requeriment.id), class: 'btn btn-primary btn-sm'